import styled from '@emotion/styled';
import { Box } from '@mui/material';

const Container = styled(Box)`
  display: flex;
  flex: 1;
`;

export function OrdersListComponent() {
  return <Container component="main">Hello world</Container>;
}
